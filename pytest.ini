[pytest]
markers =
    accumulator
    math
    rest_api
    duckduckgo
testpaths =
    tests
addopts =
    # -k "accum and not one"
    --quiet
    --maxfail=1
    --junit-xml
    failures_report.xml